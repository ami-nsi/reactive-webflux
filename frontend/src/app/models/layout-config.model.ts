export interface LayoutConfig {
  layoutCompact: boolean;
  layoutMode: string;
  darkMenu: boolean;
  profileMode: string;
  profileView: boolean;
  demoMenu: boolean;
  layoutMenu: boolean;
  globalSearch: boolean;
  footerView: boolean;
  settingsView: boolean;
  msgView: boolean;
  notiView: boolean;
  rightPanel: boolean;
  breadcrumb: boolean;
  avatar: string;
}
